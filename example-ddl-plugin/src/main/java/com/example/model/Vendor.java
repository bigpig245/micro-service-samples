package com.example.model;

/**
 * Allowed vendor for script generation without persistence.xml file.
 * At this time, only hibernate is supported.
 * <p/>
 * Vendor is required when no persistence.xml is defined.
 */
public enum Vendor {
    HIBERNATE
}
